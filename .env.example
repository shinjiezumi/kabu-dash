# docker compose 用のインフラ環境変数（cp .env.example .env で作成）
# 本番(VPS)では NGINX_SERVER_NAME を本番ドメインに変更する
NGINX_SERVER_NAME=kabu-dash.local.shinjiezumi.com

# PostgreSQLコンテナの初期化用（src/.env の DB_* と一致させること）
DB_DATABASE=kabu_dash
DB_USERNAME=kabu_dash
DB_PASSWORD=password
